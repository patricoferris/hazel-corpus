let removeDuplicates : forall a -> [a] -> [a] = typfun a -> fun l -> 
  let helper = fun (seen, rest) -> case rest 
  | [] => seen
  | h :: t => let seen' = ? in let rest' = t in helper@<a>((seen', rest'))
end in rev@<a>(helper@<a>(([], l))) in
?
